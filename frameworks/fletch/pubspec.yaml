name: httparena_fletch
description: Fletch HTTP framework — HttpArena benchmark server
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  fletch: ^2.3.0
  postgres: ^3.0.0
  sqlite3: ^3.2.0
  logger: ^2.5.0
